stages:
  - build

services:
  - docker:dind

build application:
  stage: build
  script:
    - sudo docker-compose build
    - sudo docker login -u $CI_REGISTRY_USER -p $DEPLOY_TOKEN $CI_REGISTRY 
    - sudo docker push $CI_REGISTRY_IMAGE/be-primeur-app:$CI_COMMIT_SHA
  tags:
    - shell